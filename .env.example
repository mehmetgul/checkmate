# LLM Provider Configuration
# Provider selection: "openai" (default) or "azure"
LLM_PROVIDER=openai

# Optional model overrides (defaults: gpt-4o for default tier, gpt-4o-mini for fast tier)
# LLM_MODEL_DEFAULT=gpt-4o
# LLM_MODEL_FAST=gpt-4o-mini

# SSL verification (set to "false" for enterprise proxies with self-signed certs)
# LLM_SSL_VERIFY=true

# OpenAI Configuration (required when LLM_PROVIDER=openai or not set)
OPENAI_API_KEY=your-openai-api-key
# Or use a function to get dynamic/short-lived API keys:
# LLM_API_KEY_FUNCTION=mymodule.get_openai_key

# Azure OpenAI Configuration (required when LLM_PROVIDER=azure)
# AZURE_OPENAI_API_KEY=your-azure-openai-api-key
# Or use a function for dynamic keys:
# AZURE_OPENAI_API_KEY_FUNCTION=mymodule.get_azure_key
# AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com
# AZURE_OPENAI_API_VERSION=2024-02-15-preview
# AZURE_OPENAI_DEPLOYMENT_DEFAULT=your-gpt4o-deployment
# AZURE_OPENAI_DEPLOYMENT_FAST=your-gpt4o-mini-deployment

# Database URL (SQLite for dev, PostgreSQL for prod)
DATABASE_URL=sqlite:///./qa_testing.db

# Playwright Executor Service URL
# Start the executor with: cd /usr/local/workspace/playwright-executor && uv run uvicorn executor.main:app --port 8932
PLAYWRIGHT_EXECUTOR_URL=http://localhost:8932

# Encryption key for persona passwords (Fernet key)
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=

# Smart retry
INTELLIGENT_RETRY_ENABLED=false